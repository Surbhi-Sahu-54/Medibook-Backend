server:
  port: 8087

spring:
  application:
    name: review-service
  datasource:
    url: jdbc:mysql://localhost:3306/medibook_review?createDatabaseIfNotExist=true
    username: root
    password: surbhi123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/