spring:
  datasource:
    url: jdbc:h2:mem:auth_test_db;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: root
    password: surbhi123
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
  mail:
    host: localhost
    port: 2525

eureka:
  client:
    enabled: false

app:
  upload-dir: target/test-uploads
